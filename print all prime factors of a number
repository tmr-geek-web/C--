#include<stdio.h>
int main()
{
	int n, i;
	
	printf("Enter a number:");
	scanf("%d", &n);
	
	
	for(i=2;i<=n;i++)
	{
		if(n%i==0)
		{
			printf("%d\n", i);
			while(n%i==0)
			{
				n=n/i;		//prime factorization
			}
		}
	}

    return 0;
}
